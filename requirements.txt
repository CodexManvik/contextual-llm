# Core dependencies
vosk
pyttsx3
selenium
webdriver-manager
pyautogui
pywinauto
psutil
send2trash
pywin32
python-dotenv
sounddevice
numpy
pyaudio

# Local GPU-accelerated ASR (Whisper via CTranslate2)
faster-whisper>=1.0.0

# High-quality local TTS
piper-tts>=1.2.0
onnxruntime>=1.17.0

# For testing
pytest

# Optional: For colored terminal output (if used)
colorama

# If you use LLM APIs, add their SDKs here (e.g., openai, azure-
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.20.0
sentencepiece>=0.1.99
bitsandbytes>=0.41.0
protobuf>=3.20.0
hf_xet
chromadb>=0.4.0  # For local vector stores
sentence-transformers>=2.2.0  # For embeddings
watchdog>=2.1.0  # For filesystem watching
spacy
